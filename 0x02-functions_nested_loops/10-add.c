#include "main.h"

/**
 * add - Prints the addition of two numbers
 * @a: first number
 * @b: second number
 *
 * Return: the result of adding the two numbers
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
