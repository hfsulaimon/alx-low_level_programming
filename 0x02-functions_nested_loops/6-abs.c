#include "main.h"
#include <stdlib.h>

/**
 * _abs - Prints the absolute value of an integer
 * @x: The integer to checks for its absolute value
 *
 * Return: absolute value
 */
int _abs(int x)
{
	int y = abs(x);

	return (y);
}
