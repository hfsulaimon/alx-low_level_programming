#include "main.h"

/**
 * print_last_digit - Prints last digit of a number
 * @x: The number to return its last digit
 *
 * Return: The last digit
 */
int print_last_digit(int x)
{
	int num = x % 10;

	return (num);
}

