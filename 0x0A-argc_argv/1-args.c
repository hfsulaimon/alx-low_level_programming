#include <stdio.h>
#include <stdlib.h>

/**
 * main - Starting point of C
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d", argc);
	return (0);
}
