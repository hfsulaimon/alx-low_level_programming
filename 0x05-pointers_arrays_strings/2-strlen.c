#include "main.h"
#include <string.h>

/**
  * _strlen - Returns the length of a string
  * @s: the string to check its length
  * Return: The string length
  */
int _strlen(char *s)
{
	return (strlen(s));
}
